package com.example.ui.theme

import androidx.compose.ui.graphics.Color

// Minimalist Light TickTick Palette
val TickTickBlue = Color(0xFF2563EB) // Primary TickTick calm blue
val TickTickBlueLight = Color(0xFFEFF6FF)
val TickTickBlueHover = Color(0xFF1D4ED8)

// Backgrounds & Surfaces (Soft off-white & crisp white)
val LightBg = Color(0xFFF6F8FA) // Soft off-white / light gray
val LightSurface = Color(0xFFFFFFFF) // Crisp white card surface
val LightSurfaceSubtle = Color(0xFFF1F4F9) // Subtle container gray
val LightBorder = Color(0xFFE2E8F0) // Subtle hairline border
val LightDivider = Color(0xFFEEF2F6)

// Text Colors (High contrast, readable charcoal & slate)
val TextDark = Color(0xFF0F172A) // Slate 900
val TextMedium = Color(0xFF334155) // Slate 700
val TextMutedLight = Color(0xFF64748B) // Slate 500
val TextSubtle = Color(0xFF94A3B8) // Slate 400

// Calm Quadrant Colors (Clean, professional, not neon)
val QuadrantDoFirst = Color(0xFFEF4444) // Clean Crimson / Red (Urgent & Important)
val QuadrantDoFirstLight = Color(0xFFFEF2F2)
val QuadrantSchedule = Color(0xFF2563EB) // TickTick Blue (Not Urgent & Important)
val QuadrantScheduleLight = Color(0xFFEFF6FF)
val QuadrantDelegate = Color(0xFFF59E0B) // Clean Amber (Urgent & Not Important)
val QuadrantDelegateLight = Color(0xFFFFFBEB)
val QuadrantEliminate = Color(0xFF64748B) // Slate Gray (Not Urgent & Not Important)
val QuadrantEliminateLight = Color(0xFFF8FAFC)

// Category Colors (Calm, muted productivity tones)
val CategoryAcademics = Color(0xFF0284C7) // Sky blue
val CategoryCoding = Color(0xFF0D9488) // Teal
val CategoryFitness = Color(0xFF16A34A) // Green
val CategoryContent = Color(0xFFD97706) // Ochre
val CategoryWork = Color(0xFF4F46E5) // Indigo
val CategoryPersonal = Color(0xFF9333EA) // Purple

// 3D Chart Depth Colors
val ChartShadowColor = Color(0x22000000)


